# Locales

Resource bundles for Korean (`ko`) and English (`en`) interfaces. Use nested JSON namespaces (e.g., `common`, `dashboard`, `booking`) to organize strings shared across mobile and web clients.

