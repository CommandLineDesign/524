export { colors } from './colors';
export { spacing } from './spacing';
export { borderRadius } from './borderRadius';
export { typography } from './typography';

// Maintain backward compatibility - theme object with colors and spacing
export const theme = {
  colors,
  spacing,
};
