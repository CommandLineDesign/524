/**
 * Mobile color theme
 *
 * Re-exports from the shared package to maintain backwards compatibility
 * while using a single source of truth for all colors.
 */
export { colors, primitives, statusColors, gradients, overlays } from '@524/shared';
