{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "npm run build -w @524/shared && npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
